name: mcp_images_graio_client
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/mcp_images_graio_client
  original_url: https://huggingface.co/spaces/Chris4K/mcp_images_graio_client

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"

# Updated: 2026-01-03 14:07:22
